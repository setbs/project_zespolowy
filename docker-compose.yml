services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: musicrate
      POSTGRES_USER: musicrate
      POSTGRES_PASSWORD: musicrate
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
